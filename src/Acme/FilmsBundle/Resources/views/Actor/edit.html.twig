{% extends 'AcmeFilmsBundle::base.html.twig' %}

{% block body -%}
    <h1>Actor edit</h1>

    {{ form(edit_form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('actor') }}">
                <button class="btn btn-primary">Back to the list</button>
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
